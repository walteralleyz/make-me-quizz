import React from "react";

const Footer = () => (
    <footer className="footer">
        <small>MakeMeQuizz <span>&hearts;</span> {new Date().getFullYear()} &trade;</small>
    </footer>
);

export default Footer;